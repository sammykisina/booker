const STORE_OPENING_TIME = 9; // in hours
const STORE_CLOSING_TIME = 12; // in hours
const BOOKING_TIME_INTERVAL = 30; // in mins

const config = {
  STORE_OPENING_TIME,
  STORE_CLOSING_TIME,
  BOOKING_TIME_INTERVAL,
};

export default config;
